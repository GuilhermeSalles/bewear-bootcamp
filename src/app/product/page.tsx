import { Button } from "@/components/ui/button";

const Products = () => {
  return (
    <div>
      <h1> Products </h1>
      <Button>Add Product</Button>
    </div>
  );
};

export default Products;
